\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{stmaryrd}      % \llbracket, \rrbracket
\usepackage{mathpartir}    % \inferrule for SOS rules
\usepackage{listings}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\usepackage{comment}

%% ---- notation ----
\newcommand{\Loc}{\mathit{Loc}}
\newcommand{\Var}{\mathit{Var}}
\newcommand{\Val}{\mathit{Val}}
\newcommand{\Tid}{\mathit{Tid}}
\newcommand{\Cmd}{\mathit{Cmd}}
\newcommand{\Expr}{\mathit{Expr}}
\newcommand{\store}{\sigma}
\renewcommand{\log}{\lambda}
\newcommand{\reduces}{\longrightarrow}
\newcommand{\tred}{\mathrel{\longrightarrow_{t}}}
\newcommand{\unit}{()}
\newcommand{\bbegin}{\textsf{begin}}
\newcommand{\eend}{\textsf{end}}
\newcommand{\retry}{\textsf{retry}}
\newcommand{\fork}{\textsf{fork}}
\newcommand{\getc}[1]{\textsf{get}\;(#1)}
\newcommand{\setc}[2]{\textsf{set}\,(#1,#2)}
\newcommand{\nin}{\bot}                 % "no transaction" marker
\newcommand{\subst}[3]{#1[#2\mapsto #3]}
\newcommand{\rulename}[1]{\textsc{\small #1}}

%% ---- evaluation-context notation ----
%% Evaluation contexts get their own metavariable H (script), kept distinct from
%% E (the thread->expr map). The hole is filled with plain double square
%% brackets H[[ e ]] -- the Oxford brackets [[ ]] are reserved for denotational
%% semantics, and these operational rules are not denotational.
\newcommand{\ectx}{\mathcal{H}}
\newcommand{\ehole}[1]{\ectx[[\, #1 \,]]}
%% runtime get/set actions: they record the value read/written and the
%% transaction token t, written get(l,v)@t and set(l,v)@t.
\newcommand{\getev}[3]{\textsf{get}(#1,#2)\,@\,#3}
\newcommand{\setev}[3]{\textsf{set}(#1,#2)\,@\,#3}

\newtheorem{definition}{Definition}

%% ---- declarative-semantics notation (Cerone et al. style) ----
\newcommand{\Op}{\mathit{Op}}
\newcommand{\EventId}{\mathit{EventId}}
\newcommand{\HEvent}{\mathit{HEvent}}
\newcommand{\WEvent}{\mathit{WEvent}}
\newcommand{\REvent}{\mathit{REvent}}
\newcommand{\rdev}[2]{\mathsf{read}(#1,#2)}
\newcommand{\wrev}[2]{\mathsf{write}(#1,#2)}
\newcommand{\retryev}{\rho}                 % retry event marker
\newcommand{\po}{\mathit{po}}
\newcommand{\maxpo}{\mathrm{max}_{\po}}
\newcommand{\att}{\mathit{att}}
\newcommand{\anyop}[2]{\mathord{\_}(#1,#2)} % read(x,n) or write(x,n)
\newcommand{\pob}{\xrightarrow{\po}}        % program-order edge

%% ---- listings style for the Reagent language ----
\definecolor{kw}{rgb}{0.10,0.10,0.55}
\definecolor{cm}{rgb}{0.30,0.45,0.30}
\lstdefinelanguage{reagent}{
  keywords={let,in,get,set,begin,end,retry,if,then,else},
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[n]{(*}{*)},
  morestring=[b]",
}
\lstset{
  language=reagent,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{kw}\bfseries,
  commentstyle=\color{cm}\itshape,
  showstringspaces=false,
  columns=fullflexible,
  frame=single,
  framesep=4pt,
  xleftmargin=4pt,
}
\newcommand{\code}[1]{\lstinline[basicstyle=\ttfamily]{#1}}

\title{\textbf{The Reagent Language}\\[2pt]}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
TODO
\end{abstract}




\section{Abstract syntax}

\[
\begin{array}{rcll}
n     & \in & \mathbb{N}        & \text{natural-number literals}\\
l     & \in & \Loc              & \text{shared mutable locations}\\
x     & \in & \Var              & \text{immutable, let-bound variables}\\[4pt]
v     & ::= & n \mid \unit \mid \textsf{true} \mid \textsf{false} & \text{values (naturals, unit, booleans)}\\[4pt]
c     & ::= & \getc{l}          & \text{read a shared location}\\
      & \mid & \setc{l}{e}      & \text{write a shared location}\\
      & \mid & \bbegin          & \text{open a transaction}\\
      & \mid & \eend            & \text{commit the current transaction}\\
      & \mid & \retry           & \text{abort and restart the transaction}\\[4pt]
e     & ::= & v \mid x          & \text{values, variables}\\
      & \mid & e_1 + e_2        & \text{addition (the whiteboard's }\oplus\text{)}\\
      & \mid & \textsf{let}\;x = e_1\;\textsf{in}\;e_2 & \text{binding}\\
      & \mid & c                & \text{a command (and not a control-flow)}\\
      & \mid & \textsf{if}\;e\;\textsf{then}\;e\;\textsf{else}\;e & \text{conditional}\\
\end{array}
\]

In my understanding the abstract syntax doesn't change, because \texttt{get(l,v)@t} and \texttt{set(l,v)@t} are runtime actions. The value $v$ is the value
observed at execution time and t is the active transaction token. Neither of which a programmer can write, so the source commands stay \texttt{get(l)} and \texttt{set(l,v)}.


\subsection{Concrete syntax, precedence, and lexical conventions}
\label{sec:concrete}

Keywords are \code{let in get set begin end retry if then else true false}.

%% =====================================================================
\subsection{Semantics :: of the Reagent Language}
\label{subsec:semantics}

The semantics of the Reagent language defines all possible executions of a concurrent program consisting of multiple threads interacting through a shared memory. Formally, the semantics are represented by a labeled transition system $\mathcal{S} = (\Phi,\rightarrow)$, where $\Phi$ is the set of program configurations and \(\rightarrow\) is the
transition relation generated by the rules shown in
Figure~\ref{fig:semantics}.
Each configuration $C$ in $\Phi$ represents the global state of the program at a particular point during execution. Formally, a configuration is a pentuple $C = \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle$, where:

\begin{itemize}

\item \(\sigma : \Loc \rightarrow \Val\) maps each shared memory location to its current value.

\item \(\lambda \in \Tid \cup \{\bot\}\) records the \emph{token} of the unique transaction that is currently live. $\lambda = t$ means that the transaction opened by thread $t$ (whose token is $t$) is running, and $\lambda = \bot$ means that no transaction is active. Because at most one transaction runs at any time, $\lambda$ is a single global value, not a per-thread map: the shared state should not be indexed by thread identity.

\item $E : \Tid \rightharpoonup \Expr$ maps each active thread to the
expression currently being evaluated by that thread.

\item $\hat{\sigma}: \Loc \rightarrow \Val$ is the snapshot of the shared store taken when the live transaction began; it is defined only while $\lambda \neq \bot$, and is used to support transaction restart. Since at most one transaction is active, a single snapshot suffices.

\item $\hat{E} \in \Expr$ is the expression from which execution should resume when a retry operation is encountered; like $\hat{\sigma}$ it is defined only while $\lambda \neq \bot$, and a single saved expression suffices because only one transaction is ever live.

\end{itemize}

Because the state is so lean, the rules need only a couple of premises each, so
we keep them as inference rules (premises over a conclusion transition) and lay
them out inline. In every rule the scheduled thread is $t$, with
$E(t)=\ehole{r}$ for the redex $r$; the conclusion rewrites that redex in place,
$E' = E[t \mapsto \ehole{\cdot}]$, and the label above the arrow records the
emitted action.

\begin{figure}[ht]
\small
\begin{mathpar}
\inferrule*[left=\textsc{Get}]{
  E(t)=\ehole{\getc{l}} \\ \lambda = t \\ \sigma(l)=v
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\getev{l}{v}{t}}
  \langle\sigma,\lambda,E[t\mapsto\ehole{v}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Set}]{
  E(t)=\ehole{\setc{l}{v}} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\setev{l}{v}{t}}
  \langle\sigma[l\mapsto v],\lambda,E[t\mapsto\ehole{\unit}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Begin}]{
  E(t)=\ehole{\bbegin} \\ \lambda = \bot
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\bbegin\,@\,t}
  \langle\sigma,t,E[t\mapsto\ehole{\unit}],\sigma,\ehole{\unit}\rangle
}
\and
\inferrule*[left=\textsc{End}]{
  E(t)=\ehole{\eend} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\eend\,@\,t}
  \langle\sigma,\bot,E[t\mapsto\ehole{\unit}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Retry}]{
  E(t)=\ehole{\retry} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\retry\,@\,t}
  \langle\hat{\sigma},\bot,E[t\mapsto(\bbegin\,;\,\hat{E})],\hat{\sigma},\hat{E}\rangle
}
\end{mathpar}
\caption{Semantics of Reagent commands. Each rule fires on the redex of the
scheduled thread $t$ and rewrites it in place; unlisted pentuple fields are
unchanged.}
\label{fig:semantics}
\end{figure}

Figure~\ref{fig:semantics} gives a formal description of the transition
rules. $\textrm{\sc{Get}}$ reads the value stored at a shared location
$l$. If thread $t$ holds the live transaction ($\lambda = t$) and $\sigma(l)=v$, then $\getc{l}$ returns the value $v$; the emitted action $\getev{l}{v}{t}$ records both the value $v$ that was read and the token $t$ of the transaction that read it. The store and transaction state remain unchanged.

$\textrm{\sc{Set}}$ updates a shared location. When thread $t$ (with $\lambda = t$) executes
$\setc{l}{v}$, the value stored at location $l$ is updated to $v$ and the
command returns the unit value $\unit$; the emitted action $\setev{l}{v}{t}$ records the written value $v$ and the token $t$.

$\textrm{\sc{Begin}}$ starts a transaction. No transaction may currently be alive ($\lambda = \bot$); transactions are therefore mutually exclusive, with at most one live transaction across all threads at any time. $\textrm{\sc{Begin}}$ creates a new token $t$ for the transaction and records it by setting $\lambda := t$, and the current store and expression are saved in $\hat{\sigma}$ and $\hat{E}$ respectively. These saved values can later be used by $\textsc{Retry}$.

$\textsc{End}$ commits the current transaction, resetting $\lambda$ to $\bot$. The store is unchanged.


$\textsc{Retry}$ is retrying with \emph{lock-releasing}. Instead of keeping the thread inside the transaction and spinning, it (i) rolls the store back to the snapshot $\hat{\sigma}$, so the abandoned attempt has no effect; (ii) sets $\lambda=\bot$, \emph{releasing the single critical section} so that another thread may now $\bbegin$ and run; and (iii) re-arms $t$'s expression as $\bbegin\,;\,\hat{E}$, so that when $t$ is next scheduled it must re-acquire the lock through $\textsc{Begin}$ before re-running the saved body. Because $t$ relinquishes the lock and takes a fresh snapshot on its next $\bbegin$, a write committed by another thread in the meantime is visible to the new attempt.



%% =====================================================================
\section{A memory model}
\label{sec:scheduling}

This question came up during a discussion with \textsc{Vimala}, wherein she
asked me about the memory model of \texttt{Reagents}. The following paragraph
is an attempt to capture the core idea behind it. Because I want
to ensure I have a decent understanding of the memory model  and weed out any misconceptions at a
nascent stage, I am writing it down here so that you can point out any mistakes
or gaps in my logic.


So far the rules describe \emph{which} steps are possible; they say nothing about
\emph{which thread} takes the next step. That choice belongs to a
\emph{scheduler}, which at each point picks one thread from $dom(E)$ and lets it
take a single transition. The transition relation is non-deterministic precisely
because the scheduler is free to choose; an execution of the program is one
particular sequence of scheduler choices.

Now suppose every thread runs its own transaction, i.e.\ each $E(t)$ has the
shape $\bbegin\,;\,\textit{body}_t\,;\,\eend$. Consider two threads $t_1$ and
$t_2$ and an interleaving:

\begin{itemize}
\item The scheduler picks $t_2$. Thread $t_2$ fires $\textsc{Begin}$: since
$\lambda = \bot$, the rule applies and sets $\lambda := t_2$. Thread $t_2$ has now
\emph{acquired the lock} --- it owns the single live transaction.
\item The scheduler preempts $t_2$ mid-transaction and selects $t_1$. Thread
$t_1$ is poised at its own $\bbegin$, but its $\textsc{Begin}$ rule requires
$\lambda = \bot$, whereas $\lambda = t_2$. The premise fails, so $t_1$ \emph{cannot
make progress}: there is only one lock, and it forbids $t_1$ from entering the
critical section while $t_2$ holds it. Every tx rule
$\textsc{Get}$, $\textsc{Set}$, $\textsc{End}$, $\textsc{Retry}$  carries the
premise $\lambda = t$, so the only thread that can step transactionally is the
current lock holder $t_2$.
\item Say a context switch happens, and the thread holding the
transaction, $t_2$, is brought back up. It continues until it either commits
($\textsc{End}$, which sets $\lambda := \bot$) or retries
($\textsc{Retry}$, which also releases the lock). Either way $\lambda$ returns to
$\bot$, and only then can $t_1$'s $\textsc{Begin}$ finally fire.
\end{itemize}

We assume the scheduler is \emph{fair}.

%% =====================================================================

\subsection{Evaluation contexts}

An evaluation context $\ectx$ is an expression containing a single
hole $[\cdot]$ that marks exactly where the next reduction step occurs. We give
the evaluation context its own metavariable $\ectx$ precisely to keep it
separate from $E$, which represents a mapping from thread identifiers
($\mathit{Tid}$) to expressions ($\mathit{Expr}$); the two are distinct objects
and should not be conflated.
\[
\ectx \;::=\; [\cdot]
\;\mid\; \ectx\,;\,e
\;\mid\; \textsf{let}\;x = \ectx\;\textsf{in}\;e
\;\mid\; \ectx + e \;\mid\; v + \ectx
\;\mid\; \ectx == e \;\mid\; v == \ectx
\;\mid\; \textsf{if}\;\ectx\;\textsf{then}\;e\;\textsf{else}\;e
\;\mid\; \setc{l}{\ectx}.
\]
We write $\ehole{e}$ for the expression obtained by plugging $e$ into the hole. Thus
$E(t)=\ehole{\bbegin}$ asserts that the redex thread $t$ is about to execute is
$\bbegin$, sitting inside the surrounding continuation $\ectx$.

\subsection{The command rules in context form}

Every command rule now fires on its redex inside an arbitrary context $\ectx$,
preserving the continuation. The configuration is the pentuple
$\langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle$ of
Section~\ref{subsec:semantics}.

\begin{itemize}
\item $E(t)=\ehole{\bbegin}$ and $E'=E[t\mapsto \ehole{\unit}]$: the $\bbegin$ reduces to
$\unit$ \emph{in place}, leaving the continuation $\ectx$.
\item $\hat{E}'=\ehole{\unit}$: the saved resume point is $\ehole{\unit}$,
the body \emph{after} $\bbegin$, which is precisely what $\textsc{Retry}$
reinstates.
\end{itemize}
\textsc{Retry}rolls the store back to
the snapshot $\hat{\sigma}$, releases the lock ($\lambda = \bot$), and
re-arms $t$ as $\bbegin\,;\,\hat{E}$ so that the thread must re-acquire the
lock through $\textsc{Begin}$ before re-running. I tried to capture the idea of taking a fresh snapshot in the
process. Since, $\hat{E} = \mathcal{H}_0[[\,\unit\,]]$ is the body saved by
$\textsc{Begin}$ (where $\mathcal{H}_0$ is the context at the opening $\bbegin$), the
re-armed expression $\bbegin\,;\,\hat{E}$ reconstructs the original
transaction and reduces back to $\mathcal{H}_0[[\,\bbegin\,]]$.

\subsection{Administrative reductions}


\[
\begin{aligned}
\ehole{\textsf{let}\;x = v\;\textsf{in}\;e} &\;\reduces\; \ehole{\subst{e}{x}{v}} &
\ehole{\unit \,;\, e} &\;\reduces\; \ehole{e} \\
\ehole{n_1 + n_2} &\;\reduces\; \ehole{n} \quad (n = n_1 + n_2) &
\ehole{\textsf{if}\;\textsf{true}\;\textsf{then}\;e_1\;\textsf{else}\;e_2}
   &\;\reduces\; \ehole{e_1} \\
\ehole{v_1 == v_2} &\;\reduces\; \ehole{b} \quad (b = (v_1 = v_2)) &
\ehole{\textsf{if}\;\textsf{false}\;\textsf{then}\;e_1\;\textsf{else}\;e_2}
   &\;\reduces\; \ehole{e_2}
\end{aligned}
\]
Here the notation is substitution $\subst{e}{x}{v}$ in the \textsf{let}
rather than map update.

\subsection{The running example, in context}

Take $E(t) = (\,\textsf{let}\;x = 1\;\textsf{in}\;x+1\,)\,;\,\eend$ after the
leading $\bbegin$ has been exposed; concretely the thread starts at
$E(t) = \bbegin \,;\, (\textsf{let}\;x = 1\;\textsf{in}\;x+1)\,;\,\eend$, which is
$\ehole{\bbegin}$ with
\[
\ectx \;=\; [\cdot]\,;\,(\textsf{let}\;x = 1\;\textsf{in}\;x+1)\,;\,\eend.
\]
Applying $\textsc{Begin}$:
\[
\hat{E}' = E'(t) = \ehole{\unit}
            = \unit \,;\, (\textsf{let}\;x = 1\;\textsf{in}\;x+1)\,;\,\eend,
\]
and the administrative rule $\ehole{\unit\,;\,e}\reduces \ehole{e}$ then drops the leading
$\unit$, leaving $(\textsf{let}\;x = 1\;\textsf{in}\;x+1)\,;\,\eend$ as both the
live expression and the saved resume point $\hat{E}$. The subsequent steps ::
$\textsf{let}$, then $+$, then committing at $\eend$, all proceed by plugging
into successively trailing \texttt{rest} contexts. Should a $\retry$ be reached anywhere in
the body, $\textsc{Retry}$ restores $E'(t)=\hat{E}
= (\textsf{let}\;x = 1\;\textsf{in}\;x+1)\,;\,\eend$, re-running the transaction
from just after $\bbegin$ without re-opening it.



%% =====================================================================
\section{A declarative semantics for internal consistency}
\label{sec:declarative}



%%
WIP
%%
(still trying to figure it out)
=======
Fix a single run as a history: a set of events $E$ totally ordered by program
order $\po$, with the retry events written $R(E) \subseteq E$. Because at most
one transaction is live under mutual exclusion --- the lock is held across
retries --- one such $E$ suffices, so we bind it once here and let every
quantifier below range over it.

\[
\forall e \in E.\ \forall l,v.\quad
\bigl(e = (\_,\rdev{l}{v}) \,\wedge\, \po^{-1}(e)\cap\HEvent_l \neq \emptyset\bigr)
\implies
\maxpo\bigl(\po^{-1}(e)\cap\HEvent_l\bigr) = (\_,\anyop{l}{v}),
\tag{\textsc{Int}}
\]

where $\anyop{l}{v}$ abbreviates ``a read \emph{or} a write of $v$ on $l$''.

This has been modified to the following specification.

\[
\att_l(e) \;=\; \bigl\{\, f \;\mid\; f \pob e \;\wedge\; f \in \HEvent_l \;\wedge\;
\neg\,\exists\, r \in R(E).\ f \pob r \pob e \,\bigr\}
\]
\[
\forall e \in E.\ \forall l,v.\quad
\bigl(e = (\_,\rdev{l}{v}) \,\wedge\, \att_l(e) \neq \emptyset\bigr)
\implies
\maxpo\bigl(\att_l(e)\bigr) = (\_,\anyop{l}{v}).
\tag{\textsc{Int}$^{r}$}
\]

Dropping visibility and letting a read observe the arbitration-latest earlier
writer gives the external-consistency axiom in its strict-serializable form
(\textsc{Vis} is total and coincides with \textsc{Ar}, so only \textsc{Ar}
remains):
\[
\begin{aligned}
\forall T \in H.\ \forall x,n.\quad T \vdash \mathsf{Read}\ x : n \implies {}
&\bigl(\{\, S \in H \mid S <_{\mathrm{ar}} T \,\wedge\, S \vdash \mathsf{Write}\ x : \_ \,\} = \emptyset \,\wedge\, n = 0\bigr)\\[2pt]
&{}\vee\ \mathrm{max}_{\mathrm{ar}}\{\, S \in H \mid S <_{\mathrm{ar}} T \,\wedge\, S \vdash \mathsf{Write}\ x : \_ \,\} \vdash \mathsf{Write}\ x : n,
\end{aligned}
\tag{\textsc{Ext}}
\]



WIP


\section{Abstract syntax}

\[
\begin{array}{rcll}
n     & \in & \mathbb{N}        & \text{natural-number literals}\\
l     & \in & \Loc              & \text{shared mutable locations}\\
x     & \in & \Var              & \text{immutable, let-bound variables}\\[4pt]
v     & ::= & n \mid \unit \mid \textsf{true} \mid \textsf{false} & \text{values (naturals, unit, booleans)}\\[4pt]
c     & ::= & \getc{l}          & \text{read a shared location}\\
      & \mid & \setc{l}{e}      & \text{write a shared location}\\
      & \mid & \bbegin          & \text{open a transaction}\\
      & \mid & \eend            & \text{commit the current transaction}\\
      & \mid & \retry           & \text{abort and restart the transaction}\\[4pt]
e     & ::= & v \mid x          & \text{values, variables}\\
      & \mid & e_1 + e_2        & \text{addition (the whiteboard's }\oplus\text{)}\\
      & \mid & \textsf{let}\;x = e_1\;\textsf{in}\;e_2 & \text{binding}\\
      & \mid & c                & \text{a command (and not a control-flow)}\\
      & \mid & \textsf{if}\;e\;\textsf{then}\;e\;\textsf{else}\;e & \text{conditional}\\
\end{array}
\]

In my understanding the abstract syntax doesn't change, because \texttt{get(l,v)@t} and \texttt{set(l,v)@t} are runtime actions. The value $v$ is the value
observed at execution time and t is the active transaction identifier. Neither of which a programmer can write, so the source commands stay \texttt{get(l)} and \texttt{set(l,v)}.


\subsection{Concrete syntax, precedence, and lexical conventions}
\label{sec:concrete}

Keywords are \code{let in get set begin end retry if then else true false}.

%% =====================================================================
\subsection{Semantics :: of the Reagent Language}
\label{subsec:semantics}

The semantics of the Reagent language defines all possible executions of a concurrent program consisting of multiple threads interacting through a shared memory. Formally, the semantics are represented by a labeled transition system $\mathcal{S} = (\Phi,\rightarrow)$, where $\Phi$ is the set of program configurations and \(\rightarrow\) is the
transition relation generated by the rules shown in
Figure~\ref{fig:semantics}.
Each configuration $C$ in $\Phi$ represents the global state of the program at a particular point during execution. Formally, a configuration is a pentuple $C = \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle$, where:

\begin{itemize}

\item \(\sigma : \Loc \rightarrow \mathbb{N}\) maps each shared memory location to its current value.

\item \(\lambda \in \TrId \cup \{\bot\}\) records the identifier of the unique live transaction.  If \(\lambda=t\), then transaction \(t\) is currently executing, if \(\lambda=\bot\), then no transaction is active. Because at most one transaction runs at any time, $\lambda$ is a single global value, not a per-thread map: the shared state should not be indexed by thread identity.

\item $E : \ThId \rightharpoonup \Expr$ maps each active thread to the
expression currently being evaluated by that thread.

\item $\hat{\sigma}: \Loc \rightarrow \mathbb{N}$ is the snapshot of the shared store taken when the live transaction began; it is defined only while $\lambda \neq \bot$, and is used to support transaction restart. Since at most one transaction is active, a single snapshot suffices.

\item $\hat{E} \in \Expr$ is the expression from which execution should resume when a retry operation is encountered; like $\hat{\sigma}$ it is defined only while $\lambda \neq \bot$, and a single saved expression suffices because only one transaction is ever live.

\end{itemize}

Because the state is so lean, the rules need only a couple of premises each, so we keep them as inference rules (premises over a conclusion transition) and lay them out inline. In every rule the scheduled thread is denoted by \(i\in\ThId\), while \(t\in\TrId\) denotes the currently active transaction.  Thus \(E(i)=\ehole{r}\) identifies the redex \(r\) executed by thread \(i\), the conclusion rewrites that redex in place,
\(E' = E[i\mapsto\ehole{\cdot}]\), and the transition label records the runtime action emitted by transaction identifier \(t\).

\begin{figure}[ht]
\small
\begin{mathpar}
\inferrule*[left=\textsc{Get}]{
  E(i)=\ehole{\getc{l}} \\ \lambda = t \\ \sigma(l)=v
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\getev{l}{v}{t}}
  \langle\sigma,\lambda,E[i\mapsto\ehole{v}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Set}]{
  E(i)=\ehole{\setc{l}{v}} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\setev{l}{v}{t}}
  \langle\sigma[l\mapsto v],\lambda,E[i\mapsto\ehole{\unit}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Begin}]{
  E(i)=\ehole{\bbegin\,;\,e}
  \\
  \lambda=\bot
  \\
  t_{fresh}
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\bbegin\,@\,t}
  \langle
    \sigma,
    t,
    E[i\mapsto\ehole{\unit}],
    \sigma,
    e
  \rangle
}
\and
\inferrule*[left=\textsc{End}]{
  E(i)=\ehole{\eend} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\eend\,@\,t}
  \langle\sigma,\bot,E[i\mapsto\ehole{\unit}],\hat{\sigma},\hat{E}\rangle
}
\and
\inferrule*[left=\textsc{Retry}]{
  E(i)=\ehole{\retry} \\ \lambda = t
}{
  \langle\sigma,\lambda,E,\hat{\sigma},\hat{E}\rangle
  \xrightarrow{\retry\,@\,t}
  \langle
    \hat{\sigma},
    \bot,
    E[i\mapsto\ehole{\bbegin\,;\,\hat{E}}],
    \hat{\sigma},
    \hat{E}
  \rangle
}
\end{mathpar}
\caption{Semantics of Reagent commands. 
Each rule fires on the redex of the scheduled thread $i$, the transition label records the transaction identifier $t$ and rewrites it in place; unlisted pentuple fields are
unchanged.}
\label{fig:semantics}
\end{figure}

Figure~\ref{fig:semantics} gives a formal description of the transition
rules. $\textrm{\sc{Get}}$ reads the value stored at a shared location
$l$. If thread $i$ is executing while transaction $t$ is the live transaction ($\lambda = t$) and $\sigma(l)=v$, then $\getc{l}$ returns the value $v$; the emitted action $\getev{l}{v}{t}$ records both the value $v$ that was read and the transaction identifier $t$ of the transaction that read it. The store and transaction state remain unchanged.

$\textrm{\sc{Set}}$ updates a shared location. When thread $i$ (with $\lambda = t$) executes
$\setc{l}{v}$, the value stored at location $l$ is updated to $v$ and the
command returns the unit value $\unit$; the emitted action $\setev{l}{v}{t}$ records the written value $v$ and the transaction identifier $t$.

$\textrm{\sc{Begin}}$ starts a transaction. No transaction may currently be alive ($\lambda = \bot$); transactions are therefore mutually exclusive, with at most one live transaction across all threads at any time. $\textrm{\sc{Begin}}$ allocates a fresh transaction identifier $t \in \TrId$ and records it by
setting $\lambda := t$. The executing thread $i$ now owns transaction $t$, and the current store and expression are saved in $\hat{\sigma}$ and $\hat{E}$ respectively. These saved values can later be used by $\textsc{Retry}$.

$\textsc{End}$ commits the current transaction, resetting $\lambda$ to $\bot$. The store is unchanged.

$\textsc{Retry}$ implements \emph{lock-releasing retry}. Instead of keeping thread $i$ inside the transaction and spinning, it (i) rolls the shared store back to the snapshot $\hat{\sigma}$, so that the abandoned attempt has no effect; (ii) sets $\lambda=\bot$, thereby releasing the single critical section so that another thread may execute $\bbegin$ and start a transaction; and (iii) re-arms thread $i$'s expression as $\bbegin\,;\,\hat{E}$, so that when thread $i$ is next scheduled it must re-acquire the lock by executing $\textsc{Begin}$ before re-running the saved body. Because thread $i$ relinquishes the lock and is assigned a fresh transaction identifier on its next $\bbegin$, a write committed by another thread in the meantime is visible to the new attempt.

\end{document}
